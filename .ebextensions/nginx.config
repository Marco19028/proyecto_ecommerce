files:
  "/etc/nginx/conf.d/custom.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      location ~* \.(env|ini|log)$ {
          deny all;
      }
      location ~* /vendor/ {
          deny all;
      }
      location ~* /phpunit/ {
          deny all;
      }